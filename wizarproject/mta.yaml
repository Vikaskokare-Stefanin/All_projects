_schema-version: "3.3"
ID: wizarproject
version: 0.0.1
description: "An SAP Fiori application."

parameters:
  enable-parallel-deployments: true

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci

modules:
  - name: wizarproject-ui5
    type: html5
    path: .
    build-parameters:
      builder: npm
      supported-platforms: []
      build-result: dist

resources: [] 